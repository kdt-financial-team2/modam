server:
  port: 8080

app:
  base-url: http://localhost:8080

spring:
  application:
    name: modam

  datasource:
    url: jdbc:oracle:thin:@localhost:15210/XEPDB1
    driver-class-name: oracle.jdbc.OracleDriver
    username: insurance
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false

  

  security:
    user:
      name: test
      password: 1234

  mail:
    host: smtp.gmail.com
    port: 587
    username: kbh02180@gmail.com   # Gmail 주소로 교체
    password: pjgj apso hthq trvi              # Gmail 앱 비밀번호로 교체
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

logging:
  file:
    name: logs/modam.log
  level:
    root: INFO
    "[com.intelliJ_JO.modam]": DEBUG
    "[org.springframework.security.web.authentication.rememberme]": TRACE
    "[org.springframework.security.web.context]": DEBUG
    "[org.springframework.security.web]": DEBUG

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    url: /v3/api-docs